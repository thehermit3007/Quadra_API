- name: Run with debug
  run: |
    chmod +x ./QuadraAPI.sh
    ./QuadraAPI.sh
    echo "=== CONTENIDO GENERADO ==="
    cat data.json
    echo "=== CACHE BCV ==="
    cat bcv_cache.json || echo "No existe cache"
